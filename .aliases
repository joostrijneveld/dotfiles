alias mkae=make

# main-system-specific aliasses
alias open='gio open'
alias t=todo.sh
alias pbcopy='xclip -selection clipboard'
alias pbpaste='xclip -selection clipboard -o'
alias gdiff='git diff --no-index'
alias poedit='GTK_THEME=Adwaita:light poedit'

type gp >/dev/null 2>&1 && unalias gp  # this is `git push` but shadows the pari-gp binary
